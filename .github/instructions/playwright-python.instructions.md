# Playwright Python Test Generation

## Test Writing Guidelines

- Use `get_by_role`, `get_by_label` and prefer `expect` style assertions.
- Store tests in a `tests/` directory and use `test_<feature>.py` naming.

---
description: 'Playwright Python AI test generation instructions based on official documentation.'
applyTo: '**'
