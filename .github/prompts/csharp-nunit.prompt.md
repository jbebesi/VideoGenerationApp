# NUnit Best Practices

## Project Setup

- Use a separate test project named `[ProjectName].Tests` and reference NUnit packages.
- Use `[TestFixture]` on classes and `[Test]` on methods.

## Data-Driven Tests
- Use `[TestCase]`, `[TestCaseSource]`, and `[Values]` for parameterized scenarios.

---
agent: 'agent'
